# Tier Ranking Website

This project provides a simple tier-ranking website built with React Router v7 and an Express backend.

## Development

- `npm run dev` – start Vite development server
- `npm run serve` – start Express API server
- `npm test` – run Python tests

The app lets users rank items into S–D tiers and view aggregated rankings.
